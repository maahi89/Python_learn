version: '3.8'

services:
  db:
    image: mysql:8
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: school
    ports:
      - "3306:3306"

  backend:
    build: .
    depends_on:
      - db
    ports:
      - "8000:8000"
